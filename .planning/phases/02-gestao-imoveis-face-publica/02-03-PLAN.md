# Plan 02-03: Face Pública — Homepage

**Goal:** Criar a página inicial pública da plataforma, focada em conversão, com design premium e exibição das propriedades em destaque.

---

## 1. Components — Public UI

- [x] Criar `PublicHeader` em `src/components/public/PublicHeader.tsx`:
    - Transparent top (glassmorphism) que fica sólido no scroll.
    - Menu simples (Home, Imóveis, Sobre, Contato).
    - Botão "Reserva Direta".
- [x] Criar `PublicFooter` em `src/components/public/PublicFooter.tsx`:
    - Links institucionais, redes sociais e métodos de pagamento aceitos.

---

## 2. Homepage Sections

- [x] Criar seção `Hero` em `src/app/(public)/page.tsx`:
    - Texto de alto impacto (H1).
    - Background visual premium.
    - Seletor de busca simples (apenas estética por enquanto).
- [x] Criar seção `FeaturedProperties`:
    - Query para buscar imóveis `status: 'active'` e `featured: true`.
    - Grid de `PropertyCard`.
- [x] Criar `PropertyCard` em `src/components/public/PropertyCard.tsx`:
    - Foto de capa.
    - Nome, localização, badges de capacidade.
    - Preço inicial (baseado no menor pricing sazonal ou preço base se houver).
- [x] Seção "Vantagens do Aluguel Direto":
    - Ícones e textos explicando por que não pagar taxa de OTA (Airbnb/Booking).

---

## 3. Navigation & Layout

- [x] Configurar `(public)` route group com `layout.tsx` próprio.
- [x] Garantir que o `PublicHeader` use o `PublicFooter`.

---

## Success Criteria

1. Usuário acessa a URL raiz (`/`) e vê uma página esteticamente superior.
2. Imóveis marcados como "Destaque" no admin aparecem no grid da home.
3. O design é totalmente responsivo (mobile-first).
4. Links de navegação funcionam e direcionam para as seções/páginas corretas.
